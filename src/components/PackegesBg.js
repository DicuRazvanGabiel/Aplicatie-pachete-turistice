import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class PackegesBg extends Component {
    render() {
        return (
            <View>
                <Text>BG</Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
